(() => {
  const root=document.getElementById('deckRoot'),stage=document.getElementById('stage'),slides=Array.from(stage.querySelectorAll('.slide'));
  const prevBtn=document.getElementById('prevBtn'),nextBtn=document.getElementById('nextBtn'),restartBtn=document.getElementById('restartBtn'),fullscreenBtn=document.getElementById('fullscreenBtn'),exitFullscreenBtn=document.getElementById('exitFullscreenBtn'),dots=document.getElementById('dots'),slideStatus=document.getElementById('slideStatus');
  let current=0;
  slides.forEach((_,i)=>{const b=document.createElement('button');b.type='button';b.className='dot-btn'+(i===0?' active':'');b.setAttribute('aria-label',`跳到第 ${i+1} 页`);b.addEventListener('click',()=>showSlide(i));dots.appendChild(b)});
  function showSlide(index){current=Math.max(0,Math.min(slides.length-1,index));slides.forEach((s,i)=>s.classList.toggle('active',i===current));Array.from(dots.children).forEach((d,i)=>d.classList.toggle('active',i===current));slideStatus.textContent=`${current+1} / ${slides.length}`;prevBtn.disabled=current===0;nextBtn.disabled=current===slides.length-1;stage.setAttribute('aria-label',`第 ${current+1} 页：${slides[current].dataset.title||''}`)}
  prevBtn.addEventListener('click',()=>showSlide(current-1));nextBtn.addEventListener('click',()=>showSlide(current+1));
  document.addEventListener('keydown',e=>{const tag=document.activeElement&&document.activeElement.tagName;if(['INPUT','TEXTAREA','SELECT'].includes(tag))return;if(e.key==='ArrowRight'||e.key==='PageDown'||e.key===' '){e.preventDefault();showSlide(current+1)}else if(e.key==='ArrowLeft'||e.key==='PageUp'){e.preventDefault();showSlide(current-1)}else if(e.key==='Home'){e.preventDefault();showSlide(0)}else if(e.key==='End'){e.preventDefault();showSlide(slides.length-1)}});
  async function toggleFullscreen(){try{if(document.fullscreenElement)await document.exitFullscreen();else{await root.requestFullscreen();root.focus({preventScroll:true})}}catch(_){fullscreenBtn.title='浏览器阻止了全屏，请使用浏览器自带全屏功能'}}
  if(!root.requestFullscreen){fullscreenBtn.disabled=true;fullscreenBtn.title='当前浏览器不支持全屏 API'}else{fullscreenBtn.addEventListener('click',toggleFullscreen);exitFullscreenBtn.addEventListener('click',toggleFullscreen);document.addEventListener('fullscreenchange',()=>{const on=document.fullscreenElement===root;fullscreenBtn.innerHTML=on?'⛶ <span>退出全屏</span>':'⛶ <span>全屏演示</span>';if(on)root.focus({preventScroll:true})})}
  const attributeBoard=document.getElementById('attributeBoard'),revealBtn=document.getElementById('revealBtn'),revealNote=document.getElementById('revealNote');
  revealBtn.addEventListener('click',()=>{const on=attributeBoard.classList.toggle('revealed');revealBtn.textContent=on?'隐藏 Attributes':'显示 Attributes →';revealNote.textContent=on?'这些描述维度就是 Attributes；右边的具体数值则是 Attribute Values。':'Object 本身不是一串数字；我们先选择怎样描述它。'});
  const matrix=document.getElementById('matrixTable');document.querySelectorAll('[data-matrix]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('[data-matrix]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');matrix.classList.remove('highlight-row','highlight-col');if(btn.dataset.matrix==='row')matrix.classList.add('highlight-row');if(btn.dataset.matrix==='col')matrix.classList.add('highlight-col')}));
  const valueFeedback=document.getElementById('valueFeedback');document.querySelectorAll('[data-answer]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('[data-answer]').forEach(b=>b.classList.remove('correct','wrong'));const ok=btn.dataset.answer==='no';btn.classList.add(ok?'correct':'wrong');valueFeedback.textContent=ok?'✓ 对。Height 是 Attribute；175 cm 是小明在 Height 这个 Attribute 上的 Value。':'再想一下：如果换成小雨，Height 还叫 Height，但数值会变。'}));
  document.querySelectorAll('tr[data-value]').forEach(row=>row.addEventListener('click',()=>{document.querySelectorAll('tr[data-value]').forEach(r=>r.classList.remove('selected'));row.classList.add('selected');const [a,v]=row.dataset.value.split('|');valueFeedback.textContent=`Attribute = ${a}；这个对象对应的 Attribute Value = ${v}。`}));
  const numberText={age:'年龄是数量型信息：差值通常有意义，因此很多算术操作是合理的。',id:'学号虽然由数字组成，但它的核心作用是“标识”。001 与 004 的差值通常没有分析意义。',level:'等级编码通常保留顺序，但 4 与 3 的差异不一定等同于 2 与 1。数字可能只是编码。'};const numberResult=document.getElementById('numberResult');document.querySelectorAll('[data-number]').forEach(card=>card.addEventListener('click',()=>{document.querySelectorAll('[data-number]').forEach(c=>c.classList.remove('active'));card.classList.add('active');numberResult.innerHTML=`<strong>${card.querySelector('h3').textContent}：</strong>&nbsp;${numberText[card.dataset.number]}`}));
  const featureSets={study:['出勤率','作业成绩','学习时间','前序课程成绩'],sport:['身高','体重','肺活量','50 m 成绩','心率'],consume:['消费次数','平均金额','消费时间','消费地点']};const featureList=document.getElementById('featureList');function renderFeatures(key){featureList.innerHTML=featureSets[key].map(x=>`<span class="feature-pill">${x}</span>`).join('')}renderFeatures('study');document.querySelectorAll('[data-problem]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('[data-problem]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');renderFeatures(btn.dataset.problem)}));
  const challengeFeedback=document.getElementById('challengeFeedback');document.querySelectorAll('[data-challenge]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('[data-challenge]').forEach(b=>b.classList.remove('correct','wrong'));const ok=btn.dataset.challenge==='mw';btn.classList.add(ok?'correct':'wrong');challengeFeedback.textContent=ok?'✓ 对。若目标是预测 IC50，MW / logP / HBD 可以作为输入 Features；IC50 的角色会发生变化。':'再想：如果 IC50 是我们想预测的结果，它和输入列扮演的是同一个角色吗？'}));
  function reset(){attributeBoard.classList.remove('revealed');revealBtn.textContent='显示 Attributes →';revealNote.textContent='Object 本身不是一串数字；我们先选择怎样描述它。';matrix.className='matrix highlight-row';document.querySelectorAll('[data-matrix]').forEach(b=>b.classList.toggle('active',b.dataset.matrix==='row'));document.querySelectorAll('[data-answer],[data-challenge]').forEach(b=>b.classList.remove('correct','wrong'));document.querySelectorAll('tr[data-value]').forEach(r=>r.classList.remove('selected'));valueFeedback.textContent='先作答。然后再点左边任意一行，看 Attribute 与 Value 的对应关系。';document.querySelectorAll('[data-number]').forEach(c=>c.classList.remove('active'));numberResult.innerHTML='<strong>先记住：</strong>&nbsp;“存成数字” ≠ “可以随便做数学运算”。后面还会继续讲数据类型与测量尺度。';document.querySelectorAll('[data-problem]').forEach(b=>b.classList.toggle('active',b.dataset.problem==='study'));renderFeatures('study');challengeFeedback.textContent='哪些列更像是“用来描述分子、帮助预测”的 Features？'}
  restartBtn.addEventListener('click',()=>{reset();showSlide(0);root.focus({preventScroll:true})});
  let sx=null,sy=null;stage.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse')return;sx=e.clientX;sy=e.clientY});stage.addEventListener('pointerup',e=>{if(sx===null||sy===null)return;const dx=e.clientX-sx,dy=e.clientY-sy;sx=null;sy=null;if(Math.abs(dx)>60&&Math.abs(dx)>Math.abs(dy)*1.4)showSlide(current+(dx<0?1:-1))});showSlide(0);root.focus({preventScroll:true});
})();
